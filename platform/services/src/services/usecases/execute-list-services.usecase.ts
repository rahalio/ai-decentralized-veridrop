/**
 * ExecuteListServices - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: services
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListServices Use Case
 *
 * DDD: Application use case for list Service.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListServicesInput, ListServicesOutput } from "../dto/service.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { ServiceRepository } from "../ports";

export class ExecuteListServices {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly service: ServiceRepository
  ) {}

  async execute(input: ListServicesInput): Promise<ListServicesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.svcId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.service.listServices({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListServicesOutput;
  }
}
