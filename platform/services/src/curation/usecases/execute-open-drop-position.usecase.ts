/**
 * ExecuteOpenDropPosition - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: curation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOpenDropPosition Use Case
 *
 * DDD: Application use case for get DropPosition.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OpenDropPositionInput, OpenDropPositionOutput } from "../dto/drop-position.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { DropPositionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOpenDropPosition {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly dropPosition: DropPositionRepository
  ) {}

  async execute(input: OpenDropPositionInput): Promise<OpenDropPositionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.curId();

    // All field names extracted from OpenAPI spec: curatorParticipantId, dropsAmount, serviceId, stakedOcean
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).curatorParticipantId) {
      throw new ValidationError("curatorParticipantId is required");
    }
    const curatorparticipantId = (input as any).curatorParticipantId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.dropPosition.openDropPosition({ curatorParticipantId: curatorparticipantId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${curatorparticipantId}`);
    }

    return entity as OpenDropPositionOutput;
  }
}
