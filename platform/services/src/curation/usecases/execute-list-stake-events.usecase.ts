/**
 * ExecuteListStakeEvents - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: curation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListStakeEvents Use Case
 *
 * DDD: Application use case for list StakeEvent.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListStakeEventsInput, ListStakeEventsOutput } from "../dto/stake-event.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { StakeEventRepository } from "../ports";

export class ExecuteListStakeEvents {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly stakeEvent: StakeEventRepository
  ) {}

  async execute(input: ListStakeEventsInput): Promise<ListStakeEventsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.curId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.stakeEvent.listStakeEvents({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListStakeEventsOutput;
  }
}
