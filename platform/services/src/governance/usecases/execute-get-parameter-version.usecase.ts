/**
 * ExecuteGetParameterVersion - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetParameterVersion Use Case
 *
 * DDD: Application use case for get ParameterVersion.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetParameterVersionInput, GetParameterVersionOutput } from "../dto/parameter-version.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { ParameterVersionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetParameterVersion {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly parameterVersion: ParameterVersionRepository
  ) {}

  async execute(input: GetParameterVersionInput): Promise<GetParameterVersionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.govId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).parameterVersionId) {
      throw new ValidationError("parameterVersionId is required");
    }
    const parameterversionId = (input as any).parameterVersionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.parameterVersion.getParameterVersion({ parameterVersionId: parameterversionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${parameterversionId}`);
    }

    return entity as GetParameterVersionOutput;
  }
}
