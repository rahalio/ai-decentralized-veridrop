/**
 * ExecuteProposeParameterVersion - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteProposeParameterVersion Use Case
 *
 * DDD: Application use case for get ParameterVersion.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ProposeParameterVersionInput, ProposeParameterVersionOutput } from "../dto/parameter-version.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { ParameterVersionRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteProposeParameterVersion {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly parameterVersion: ParameterVersionRepository
  ) {}

  async execute(input: ProposeParameterVersionInput): Promise<ProposeParameterVersionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.govId();

    // All field names extracted from OpenAPI spec: delayHours, kind, payloadDigest, summary
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.parameterVersion.proposeParameterVersion({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as ProposeParameterVersionOutput;
  }
}
