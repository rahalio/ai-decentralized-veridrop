/**
 * ExecuteGetDeliveryProof - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: proofs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetDeliveryProof Use Case
 *
 * DDD: Application use case for get DeliveryProof.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetDeliveryProofInput, GetDeliveryProofOutput } from "../dto/delivery-proof.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { DeliveryProofRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetDeliveryProof {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly deliveryProof: DeliveryProofRepository
  ) {}

  async execute(input: GetDeliveryProofInput): Promise<GetDeliveryProofOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prfId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).proofId) {
      throw new ValidationError("proofId is required");
    }
    const proofId = (input as any).proofId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.deliveryProof.getDeliveryProof({ proofId: proofId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${proofId}`);
    }

    return entity as GetDeliveryProofOutput;
  }
}
