/**
 * ExecuteApplyParticipant - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: participants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteApplyParticipant Use Case
 *
 * DDD: Application use case for get Participant.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ApplyParticipantInput, ApplyParticipantOutput } from "../dto/participant.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { ParticipantRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteApplyParticipant {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly participant: ParticipantRepository
  ) {}

  async execute(input: ApplyParticipantInput): Promise<ApplyParticipantOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ptcId();

    // All field names extracted from OpenAPI spec: displayName, roles
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.participant.applyParticipant({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as ApplyParticipantOutput;
  }
}
