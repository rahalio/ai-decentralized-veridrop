/**
 * ExecuteGetRewardEpoch - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: rewards
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetRewardEpoch Use Case
 *
 * DDD: Application use case for get RewardEpoch.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetRewardEpochInput, GetRewardEpochOutput } from "../dto/reward-epoch.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veridrop/services/_shared/index.js";
import type { RewardEpochRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetRewardEpoch {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly rewardEpoch: RewardEpochRepository
  ) {}

  async execute(input: GetRewardEpochInput): Promise<GetRewardEpochOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rwdId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).epochId) {
      throw new ValidationError("epochId is required");
    }
    const epochId = (input as any).epochId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.rewardEpoch.getRewardEpoch({ epochId: epochId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${epochId}`);
    }

    return entity as GetRewardEpochOutput;
  }
}
