/**
 * Service Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: services
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { ServiceEntity } from "@veridrop/core/services/models/index.js";
import { servicesSchemas } from "@veridrop/core/services";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "svc";

export function createService(overrides: Partial<ServiceEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    pricingMode: overrides.pricingMode ?? "commons",
    proofTypeRequired: overrides.proofTypeRequired ?? "availability",
    providerParticipantId: overrides.providerParticipantId ?? faker.string.uuid(),
    serviceId: overrides.serviceId ?? `svc_${ulid().toLowerCase()}`,
    serviceType: overrides.serviceType ?? "dataAvailability",
    status: overrides.status ?? "active",
    ...overrides,
  };

  return servicesSchemas.Service.parse(raw);
}

export function serviceBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createService(overrides));
}
