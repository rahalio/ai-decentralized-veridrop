/**
 * ServiceAgreement Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: agreements
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { ServiceAgreementEntity } from "@veridrop/core/agreements/models/index.js";
import { agreementsSchemas } from "@veridrop/core/agreements";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "agr";

export function createServiceAgreement(overrides: Partial<ServiceAgreementEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    agreementId: overrides.agreementId ?? `agr_${ulid().toLowerCase()}`,
    consumerParticipantId: overrides.consumerParticipantId ?? faker.string.uuid(),
    serviceId: overrides.serviceId ?? faker.string.uuid(),
    status: overrides.status ?? "proposed",
    ...overrides,
  };

  return agreementsSchemas.ServiceAgreement.parse(raw);
}

export function serviceAgreementBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createServiceAgreement(overrides));
}
