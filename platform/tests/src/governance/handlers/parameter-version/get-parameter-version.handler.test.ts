/**
 * GetParameterVersion handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getParameterVersion } from "@veridrop/api-server/domains/governance/handlers";
import type { GovernanceDDDDependencies } from "@veridrop/api-server/domains/governance";
import { createParameterVersion as createParameterVersionEntity } from "@veridrop/factories/governance";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veridrop/tests/shared";
import { GOVERNANCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetParameterVersion", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GovernanceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GovernanceDDDDependencies>(
      GOVERNANCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createParameterVersionEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetParameterVersion, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { parameterVersionId: entity.id };

    await getParameterVersion(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetParameterVersion.execute).toHaveBeenCalledWith(expect.objectContaining({ parameterVersionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { parameterVersionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetParameterVersion, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getParameterVersion(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
