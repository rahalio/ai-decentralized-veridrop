/**
 * GetRewardEpoch handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: rewards
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getRewardEpoch } from "@veridrop/api-server/domains/rewards/handlers";
import type { RewardsDDDDependencies } from "@veridrop/api-server/domains/rewards";
import { createRewardEpoch as createRewardEpochEntity } from "@veridrop/factories/rewards";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veridrop/tests/shared";
import { REWARDS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetRewardEpoch", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RewardsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RewardsDDDDependencies>(
      REWARDS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createRewardEpochEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetRewardEpoch, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { epochId: entity.id };

    await getRewardEpoch(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetRewardEpoch.execute).toHaveBeenCalledWith(expect.objectContaining({ epochId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { epochId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetRewardEpoch, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getRewardEpoch(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
