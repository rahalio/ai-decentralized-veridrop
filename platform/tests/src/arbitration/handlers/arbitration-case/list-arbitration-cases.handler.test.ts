/**
 * ListArbitrationCases handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: arbitration
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listArbitrationCases } from "@veridrop/api-server/domains/arbitration/handlers";
import type { ArbitrationDDDDependencies } from "@veridrop/api-server/domains/arbitration";
import { createArbitrationCase as createArbitrationCaseEntity } from "@veridrop/factories/arbitration";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veridrop/tests/shared";
import { ARBITRATION_USE_CASES } from "../__factories__/use-cases.js";

describe("ListArbitrationCases", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ArbitrationDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ArbitrationDDDDependencies>(
      ARBITRATION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createArbitrationCaseEntity({ orgId }),
      createArbitrationCaseEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListArbitrationCases, "execute").mockResolvedValue(useCaseResult);

    await listArbitrationCases(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListArbitrationCases.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
