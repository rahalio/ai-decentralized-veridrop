/**
 * Proofs Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: proofs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const submitDeliveryProof_Body = z
  .object({
    serviceId: z.string(),
    agreementId: z.string(),
    proofType: z.enum(['availability', 'zkCompute', 'replication']),
    proofDigest: z.string(),
    keeperParticipantId: z.string().optional(),
  })
  .passthrough();

export const schemas: any = {
  submitDeliveryProof_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v0/delivery-proofs',
    alias: 'listDeliveryProofs',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(100).optional().default(25),
      },
      {
        name: 'verdict',
        type: 'Query',
        schema: z
          .enum(['pending', 'accepted', 'rejected', 'slashed'])
          .optional(),
      },
      {
        name: 'serviceId',
        type: 'Query',
        schema: z.string().optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
                  serviceId: z.string(),
                  agreementId: z.string(),
                  proofType: z.enum([
                    'availability',
                    'zkCompute',
                    'replication',
                  ]),
                  verdict: z.enum([
                    'pending',
                    'accepted',
                    'rejected',
                    'slashed',
                  ]),
                  submittedAt: z.string().datetime({ offset: true }),
                  decidedAt: z.string().datetime({ offset: true }).optional(),
                  rejectReason: z.string().optional(),
                  slashReason: z.string().optional(),
                  proofDigest: z.string().optional(),
                  keeperParticipantId: z.string().optional(),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v0/delivery-proofs',
    alias: 'submitDeliveryProof',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: submitDeliveryProof_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
            serviceId: z.string(),
            agreementId: z.string(),
            proofType: z.enum(['availability', 'zkCompute', 'replication']),
            verdict: z.enum(['pending', 'accepted', 'rejected', 'slashed']),
            submittedAt: z.string().datetime({ offset: true }),
            decidedAt: z.string().datetime({ offset: true }).optional(),
            rejectReason: z.string().optional(),
            slashReason: z.string().optional(),
            proofDigest: z.string().optional(),
            keeperParticipantId: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v0/delivery-proofs/:proofId',
    alias: 'getDeliveryProof',
    requestFormat: 'json',
    parameters: [
      {
        name: 'proofId',
        type: 'Path',
        schema: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
            serviceId: z.string(),
            agreementId: z.string(),
            proofType: z.enum(['availability', 'zkCompute', 'replication']),
            verdict: z.enum(['pending', 'accepted', 'rejected', 'slashed']),
            submittedAt: z.string().datetime({ offset: true }),
            decidedAt: z.string().datetime({ offset: true }).optional(),
            rejectReason: z.string().optional(),
            slashReason: z.string().optional(),
            proofDigest: z.string().optional(),
            keeperParticipantId: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/delivery-proofs/:proofId/accept',
    alias: 'acceptDeliveryProof',
    requestFormat: 'json',
    parameters: [
      {
        name: 'proofId',
        type: 'Path',
        schema: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
            serviceId: z.string(),
            agreementId: z.string(),
            proofType: z.enum(['availability', 'zkCompute', 'replication']),
            verdict: z.enum(['pending', 'accepted', 'rejected', 'slashed']),
            submittedAt: z.string().datetime({ offset: true }),
            decidedAt: z.string().datetime({ offset: true }).optional(),
            rejectReason: z.string().optional(),
            slashReason: z.string().optional(),
            proofDigest: z.string().optional(),
            keeperParticipantId: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/delivery-proofs/:proofId/reject',
    alias: 'rejectDeliveryProof',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: z
          .object({ rejectReason: z.string().min(1).max(500) })
          .passthrough(),
      },
      {
        name: 'proofId',
        type: 'Path',
        schema: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
            serviceId: z.string(),
            agreementId: z.string(),
            proofType: z.enum(['availability', 'zkCompute', 'replication']),
            verdict: z.enum(['pending', 'accepted', 'rejected', 'slashed']),
            submittedAt: z.string().datetime({ offset: true }),
            decidedAt: z.string().datetime({ offset: true }).optional(),
            rejectReason: z.string().optional(),
            slashReason: z.string().optional(),
            proofDigest: z.string().optional(),
            keeperParticipantId: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/delivery-proofs/:proofId/slash',
    alias: 'slashDeliveryProof',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: z
          .object({ slashReason: z.string().min(1).max(500) })
          .passthrough(),
      },
      {
        name: 'proofId',
        type: 'Path',
        schema: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            proofId: z.string().regex(/^prf_[0-9A-HJKMNP-TV-Z]{26}$/),
            serviceId: z.string(),
            agreementId: z.string(),
            proofType: z.enum(['availability', 'zkCompute', 'replication']),
            verdict: z.enum(['pending', 'accepted', 'rejected', 'slashed']),
            submittedAt: z.string().datetime({ offset: true }),
            decidedAt: z.string().datetime({ offset: true }).optional(),
            rejectReason: z.string().optional(),
            slashReason: z.string().optional(),
            proofDigest: z.string().optional(),
            keeperParticipantId: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios('https://api.veridrop.local', endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
