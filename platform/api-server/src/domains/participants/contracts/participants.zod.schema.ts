/**
 * Participants Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: participants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const applyParticipant_Body = z
  .object({
    displayName: z.string().min(1).max(200),
    roles: z
      .array(
        z.enum([
          'operator',
          'keeper',
          'provider',
          'curator',
          'consumer',
          'arbitrator',
          'auditor',
        ])
      )
      .min(1),
  })
  .passthrough();
const approveParticipantRole_Body = z
  .object({
    roles: z
      .array(
        z.enum([
          'operator',
          'keeper',
          'provider',
          'curator',
          'consumer',
          'arbitrator',
          'auditor',
        ])
      )
      .min(1),
  })
  .passthrough();

export const schemas: any = {
  applyParticipant_Body,
  approveParticipantRole_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v0/participants',
    alias: 'listParticipants',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(100).optional().default(25),
      },
      {
        name: 'tcrStatus',
        type: 'Query',
        schema: z
          .enum(['applied', 'listed', 'challenged', 'rejected'])
          .optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  participantId: z
                    .string()
                    .regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
                  displayName: z.string().max(200),
                  roles: z
                    .array(
                      z.enum([
                        'operator',
                        'keeper',
                        'provider',
                        'curator',
                        'consumer',
                        'arbitrator',
                        'auditor',
                      ])
                    )
                    .min(1),
                  tcrStatus: z.enum([
                    'applied',
                    'listed',
                    'challenged',
                    'rejected',
                  ]),
                  eligibilityNote: z.string().optional(),
                  challengeReason: z.string().optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v0/participants',
    alias: 'applyParticipant',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: applyParticipant_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            participantId: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
            displayName: z.string().max(200),
            roles: z
              .array(
                z.enum([
                  'operator',
                  'keeper',
                  'provider',
                  'curator',
                  'consumer',
                  'arbitrator',
                  'auditor',
                ])
              )
              .min(1),
            tcrStatus: z.enum(['applied', 'listed', 'challenged', 'rejected']),
            eligibilityNote: z.string().optional(),
            challengeReason: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v0/participants/:participantId',
    alias: 'getParticipant',
    requestFormat: 'json',
    parameters: [
      {
        name: 'participantId',
        type: 'Path',
        schema: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            participantId: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
            displayName: z.string().max(200),
            roles: z
              .array(
                z.enum([
                  'operator',
                  'keeper',
                  'provider',
                  'curator',
                  'consumer',
                  'arbitrator',
                  'auditor',
                ])
              )
              .min(1),
            tcrStatus: z.enum(['applied', 'listed', 'challenged', 'rejected']),
            eligibilityNote: z.string().optional(),
            challengeReason: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/participants/:participantId/approve-role',
    alias: 'approveParticipantRole',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: approveParticipantRole_Body,
      },
      {
        name: 'participantId',
        type: 'Path',
        schema: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            participantId: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
            displayName: z.string().max(200),
            roles: z
              .array(
                z.enum([
                  'operator',
                  'keeper',
                  'provider',
                  'curator',
                  'consumer',
                  'arbitrator',
                  'auditor',
                ])
              )
              .min(1),
            tcrStatus: z.enum(['applied', 'listed', 'challenged', 'rejected']),
            eligibilityNote: z.string().optional(),
            challengeReason: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v0/participants/:participantId/challenge',
    alias: 'challengeParticipant',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: z.object({ reason: z.string().min(1).max(1000) }).passthrough(),
      },
      {
        name: 'participantId',
        type: 'Path',
        schema: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            participantId: z.string().regex(/^ptc_[0-9A-HJKMNP-TV-Z]{26}$/),
            displayName: z.string().max(200),
            roles: z
              .array(
                z.enum([
                  'operator',
                  'keeper',
                  'provider',
                  'curator',
                  'consumer',
                  'arbitrator',
                  'auditor',
                ])
              )
              .min(1),
            tcrStatus: z.enum(['applied', 'listed', 'challenged', 'rejected']),
            eligibilityNote: z.string().optional(),
            challengeReason: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
]);

export const api: any = new Zodios('https://api.veridrop.local', endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
