/**
 * Curation View
 *
 * Main view component for curation domain.
 */

import { CurationViewProps } from "./types";

export function CurationView({}: CurationViewProps) {
  return (
    <div>
      <h1>Curation View</h1>
      <p>This is a generated view component for the curation domain.</p>
      {/* TODO: Implement view */}
    </div>
  );
}
